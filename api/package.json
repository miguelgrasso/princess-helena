{
  "name": "helena-api",
  "version": "1.0.0",
  "private": true,
  "description": "API del leaderboard global de Princess Helena",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "migrate": "node src/migrate.js",
    "dev": "node --watch src/server.js",
    "dev:serve": "node dev-server.js"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.1",
    "@fastify/rate-limit": "^10.2.1",
    "@prometheus-io/client": "^0.16.1",
    "fastify": "^5.2.0",
    "pg": "^8.13.1"
  }
}
